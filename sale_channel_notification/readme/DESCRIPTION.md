This module adds the posibility to customize odoo notifications per channel.

To enable custom notifications for a channel, go to
`Sale > Configuration > Sale Channels` and check the "Custom Notifications" option.

You will now be able to set up a new sale channel notification for an action in
the Notification tab that should appear.
